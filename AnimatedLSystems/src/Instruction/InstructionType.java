package Instruction;

public enum  InstructionType {
    LINE

    //types would be like draw vs move vs place vs make a continuous action
    //in the grammar thing certain steps would have to have certain symbols following it
}
